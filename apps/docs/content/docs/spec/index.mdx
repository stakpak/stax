---
title: "Overview"
description: "Core concepts and scope of the stax distribution standard"
---

## What is stax?

stax is a **distribution standard** for AI agents. It defines how to describe, bundle, version, validate, verify, and distribute agent artifacts as OCI artifacts.

stax artifacts are the agent equivalent of `docker build`, `docker push`, and `docker pull`: authors build and publish immutable agent artifacts, and consumers pull, inspect, verify, materialize, install, or import them into the environments where agents run.

stax does **not** run agents, orchestrate them, or manage their lifecycle. Those concerns belong to runtimes, orchestrators, IDEs, CLIs, hosted agent platforms, and cloud systems built on top of the format.

stax is to agents what the OCI Image Spec is to containers, what `package.json` is to JavaScript packages, and what `Chart.yaml` is to Helm charts.

In the current market, stax is best understood first as a standard for **coding-agent asset distribution**. The broader vision still applies, but the most mature current fit is for instructions, instruction trees, subagents, skills, MCP config, package reuse, policy, and promotion across developer-facing agent runtimes.

## Specification status

<Callout type="info" title="Version 1.0.0">
  This specification describes **stax spec version `1.0.0`**. All compiled JSON payloads defined by
  stax SHOULD include `specVersion: "1.0.0"` unless the relevant layer format says otherwise.
</Callout>

### Normative language

The key words **MUST**, **MUST NOT**, **REQUIRED**, **SHOULD**, **SHOULD NOT**, and **MAY** in this specification are to be interpreted as described in RFC 2119 / RFC 8174.

## Key Spec Sections

<Cards>
  <Card
    title="Agent Manifest"
    href="/docs/spec/agent-manifest"
    description="The root manifest and compiled config blob (Spec 01)"
  />
  <Card
    title="OCI Artifact Format"
    href="/docs/spec/oci-artifact-format"
    description="OCI manifests, media types, annotations, and referrers (Spec 02)"
  />
  <Card
    title="Layers"
    href="/docs/spec/layers"
    description="Deterministic layer construction and validation (Spec 03)"
  />
  <Card
    title="Packages"
    href="/docs/spec/packages"
    description="Shared config bundles, dependency resolution, and merge rules (Spec 05)"
  />
  <Card
    title="Adapters"
    href="/docs/spec/adapters"
    description="Runtime adapters and feature contracts (Spec 12)"
  />
  <Card
    title="Materialization"
    href="/docs/spec/materialization"
    description="Consumer contract for translating artifacts to runtime-native files (Spec 15)"
  />
</Cards>

## Scope

### What stax defines

- **Authoring formats** for agents, packages, personas, prompt surfaces, MCP, rules, skills, knowledge, memory, and secrets
- **A compiled config blob** stored in OCI
- **Distribution semantics** for versioning, addressing, inspection, pull/push workflows, and reproducible builds
- **Canonical subagent bundles** for named delegate agents and agent-role packaging
- **Path-scoped instruction trees** for runtimes that discover instructions hierarchically
- **Runtime profile artifacts** for portable non-secret runtime configuration when appropriate
- **Source artifacts** for cacheable workspace snapshots shared across many agents
- **Typed OCI layers** for each brain concern
- **Deterministic build rules** for packaged directories and compiled JSON
- **Package dependency and merge rules**
- **Adapter metadata** describing how a consumer materializes an artifact for a runtime
- **A consumer/materialization contract** for turning canonical layers into runtime-native files, API payloads, bundles, or remote objects
- **A baseline compatibility and install-plan contract** for preflight checks before files or remote objects are changed
- **A CLI contract** for build, validate, inspect, plan, materialize, push, pull, and verify workflows
- **Referrer conventions** for signatures, evaluations, approvals, and memory snapshots

### What stax does NOT define

These remain out of scope and are owned by consumers of stax artifacts.

| Concern                    | Why it's out of scope                                                  | Who handles it                            |
| -------------------------- | ---------------------------------------------------------------------- | ----------------------------------------- |
| Running agents             | stax describes what to run, not how                                    | CLIs, IDEs, platforms, orchestrators      |
| Long-running lifecycle     | Start, resume, suspend, shutdown, and job control are runtime concerns | Hosted runtimes, orchestrators, platforms |
| Swarm topology             | Multi-agent relationships are orchestration concerns                   | Agent orchestrators                       |
| Replicas and scaling       | Deployment behavior is operational                                     | Orchestrators, K8s                        |
| Resource limits            | CPU / memory allocation is runtime infrastructure                      | Container runtimes, microVMs              |
| Secret resolution          | stax declares needs, not providers or values                           | Vaults, env injection, orchestrators      |
| Retry policy               | Execution policy is runtime-specific                                   | Consumers                                 |
| Monitoring / tracing       | Observability is infrastructure                                        | Platforms, APM tools                      |
| Transport between agents   | Messaging is orchestration                                             | Message buses, platforms                  |
| Access control enforcement | Policy enforcement is runtime-specific                                 | IAM, sandboxing, network layers           |

## Boundary principle

> A stax artifact carries **what an agent is** and **what it needs to start**.
> Everything about **where it runs, how long it runs, with what limits, under what policies, and in what topology** is extrinsic and belongs to the consumer.

This is the same separation used by successful packaging standards:

- OCI images describe the image, not the deployment topology
- npm packages describe dependencies, not process supervision
- Helm charts describe desired resources, not cluster implementation details

## Core concepts

<Accordions>
  <Accordion title="Agent artifact">
    A versioned OCI artifact containing an agent's canonical distribution payload: identity,
    adapter, prompt, persona, rules, MCP servers, skills, knowledge, memory seed, secret
    declarations, and resolved package metadata.
  </Accordion>
  <Accordion title="Package artifact">
    A reusable OCI artifact that contributes canonical layers such as MCP, skills, rules, knowledge,
    and secrets. Packages are merged into agents at build or materialization time according to the
    package merge rules.
  </Accordion>
  <Accordion title="Runtime profile artifact">
    A separate OCI artifact for runtime-scoped, non-secret configuration that should not live inside
    an agent brain. For example: portable OpenClaw `openclaw.json` defaults packaged via
    `@stax/openclaw/profile`.
  </Accordion>
  <Accordion title="Source artifact">
    A separate OCI artifact containing a cacheable workspace snapshot, typically derived from a Git
    repository or source archive. Agents reference source artifacts instead of embedding full repos
    in every brain artifact.
  </Accordion>
  <Accordion title="Config blob">
    The OCI `config` object for an agent or package. This is the canonical compiled manifest and
    includes identity, adapter metadata, runtime hints, package references, and references to
    optional layers.
  </Accordion>
  <Accordion title="Layer">
    A typed OCI layer containing one logical concern, such as persona JSON or a gzipped tarball of
    skills.
  </Accordion>
  <Accordion title="Adapter">
    A runtime-specific description of how a canonical stax agent maps to a target runtime such as
    Claude Code, Codex, or OpenClaw. Future adapters MAY target other runtimes such as Cursor,
    Windsurf, or hosted platforms.
  </Accordion>
  <Accordion title="Subagent bundle">
    A canonical bundle of named delegate agents attached to a primary agent artifact. Subagent
    bundles describe agent roles, invocation intent, and handoff metadata without defining
    orchestration topology.
  </Accordion>
  <Accordion title="Instruction tree">
    A canonical path-scoped instruction hierarchy that preserves per-directory or per-scope
    instruction documents for runtimes that discover instructions by path rather than from a single
    root file.
  </Accordion>
  <Accordion title="Consumer">
    Any tool that reads stax artifacts: a CLI, IDE plugin, runtime, orchestrator, registry-aware
    service, hosted agent platform, or cloud control plane.
  </Accordion>
  <Accordion title="Materialization">
    The process of turning a canonical stax artifact into runtime-native files, settings, API
    payloads, bundles, or import objects. For example: rendering prompt templates, translating MCP
    to `.mcp.json`, writing skills into a runtime-specific directory, or preparing a hosted platform
    import bundle.
  </Accordion>
  <Accordion title="Install plan">
    A machine-readable preview of what a consumer would pull, verify, materialize, or import for a
    selected runtime or policy context, including compatibility and fidelity reasoning.
  </Accordion>
</Accordions>

## Compatibility model

### Spec compatibility

- A consumer that implements stax `1.x` MUST reject artifacts with a higher unsupported **major** `specVersion`.
- A consumer SHOULD ignore unknown additive fields within the same supported major version unless a layer spec says the field is strict.
- Removing or changing the meaning of an existing field is a breaking change and requires a new major spec version.
- Adding optional fields is non-breaking.

### Forward compatibility

- A consumer encountering an unknown OCI layer media type within a stax artifact MUST skip the layer and SHOULD warn. Unknown layers MUST NOT cause a build or materialization failure.
- A consumer encountering unknown fields in a config blob or JSON layer MUST ignore them and SHOULD warn, unless the field is in a namespace the consumer explicitly manages.
- Builders SHOULD NOT strip unknown fields from JSON layers or config blobs when re-packaging or copying artifacts.

### Deprecation process

To deprecate a field or feature in a future minor version:

1. The spec MUST document the field as deprecated with a target removal version
2. Builders SHOULD warn when deprecated fields are used
3. Deprecated fields MUST NOT be removed until a new major spec version
4. Consumers MUST continue to accept deprecated fields for the entire major version

### Adapter compatibility

Adapters MUST declare:

- `type` — adapter identifier (for example `claude-code`)
- `runtime` — target runtime family (for example `claude-code`)
- `adapterVersion` — adapter schema version
- `features` — supported translation features

Consumers SHOULD warn when an artifact uses features unsupported by the selected adapter or runtime.

Consumers SHOULD also expose a machine-readable compatibility and install-impact summary before applying changes when the selected operation would write files or mutate a remote consumer.

## Determinism principle

stax artifacts are intended to be **reproducible**.

Two conforming builders given the same source tree, ignore rules, dependency lockfile, and build options SHOULD produce identical OCI layer digests.

Deterministic packaging rules are defined in [03 — Layers](./03-layers.md).

## Architecture

```text title="Architecture"
┌──────────────────────────────────────────────────┐
│                     stax                         │
│              (distribution standard)             │
│                                                  │
│  TypeScript SDK → OCI Artifact → Registry        │
│                                                  │
│  defineAgent()     Typed layers    Push/Pull     │
│  definePackage()   Config blob     Inspect       │
│  definePersona()   Lockfile        Materialize   │
└──────────────────┬───────────────────────────────┘
                   │
                   │ OCI artifacts consumed by:
                   │
     ┌─────────────┼─────────────┬─────────────────┐
     ▼             ▼             ▼                 ▼
  Your           Agent         Cloud           Community
  Orchestrator   Platforms     Providers       Tools
```

## Conformance levels

<Tabs items={['Builder', 'Consumer', 'Registry Tool']}>
  <Tab value="Builder">
    A builder compiles authoring files into canonical JSON and deterministic OCI artifacts.
  </Tab>
  <Tab value="Consumer">
    A consumer reads artifacts, validates them, resolves packages, and optionally materializes them for a runtime.
  </Tab>
  <Tab value="Registry Tool">
    A registry tool pushes, pulls, copies, signs, verifies, and inspects artifacts and referrers.

    A single implementation MAY satisfy one, two, or all three roles.

  </Tab>
</Tabs>

## Specification Documents

<Callout type="info" title="Normative vs. Draft">
  Specs `01`-`22` and `33`-`38` define the current normative `1.0.0` surface. Specs `23`-`32` are
  forward-looking drafts and are not part of `1.0.0` conformance. See [99 — Roadmap and Draft
  Status](./99-roadmap.md) for the status map.
</Callout>

### Core Specs

<Cards>
  <Card
    title="01 — Agent Manifest"
    href="/docs/spec/agent-manifest"
    description="The root manifest and compiled config blob"
  />
  <Card
    title="02 — OCI Artifact Format"
    href="/docs/spec/oci-artifact-format"
    description="OCI manifests, media types, annotations, and referrers"
  />
  <Card
    title="03 — Layers"
    href="/docs/spec/layers"
    description="Deterministic layer construction and validation"
  />
  <Card
    title="04 — Persona"
    href="/docs/spec/persona"
    description="Persona format, validation, and prompt templating"
  />
  <Card
    title="05 — Packages"
    href="/docs/spec/packages"
    description="Shared configuration bundles, dependency resolution, and merge rules"
  />
  <Card
    title="16 — Surfaces"
    href="/docs/spec/surfaces"
    description="Named prompt/instruction documents for exact runtime file mapping"
  />
</Cards>

### Layer Specs

<Cards>
  <Card title="06 — MCP" href="/docs/spec/mcp" description="Canonical MCP server definitions" />
  <Card
    title="07 — Skills"
    href="/docs/spec/skills"
    description="Skill packaging and frontmatter schema"
  />
  <Card title="08 — Rules" href="/docs/spec/rules" description="Canonical behavioral rules" />
  <Card
    title="09 — Knowledge"
    href="/docs/spec/knowledge"
    description="Knowledge packaging and metadata"
  />
  <Card
    title="10 — Memory"
    href="/docs/spec/memory"
    description="Seed memory and memory snapshot conventions"
  />
  <Card title="11 — Secrets" href="/docs/spec/secrets" description="Secret declaration semantics" />
</Cards>

### Runtime and CLI

<Cards>
  <Card
    title="12 — Adapters"
    href="/docs/spec/adapters"
    description="Runtime adapters and feature contracts"
  />
  <Card title="13 — CLI" href="/docs/spec/cli" description="Reference CLI behavior" />
  <Card title="14 — Use Cases" href="/docs/spec/use-cases" description="Example scenarios" />
  <Card
    title="15 — Materialization"
    href="/docs/spec/materialization"
    description="Consumer contract for translating artifacts to runtime-native files"
  />
  <Card
    title="17 — Runtime File Contracts"
    href="/docs/spec/runtime-file-contracts"
    description="Exact file ownership and target mappings for runtime homes"
  />
</Cards>

### Adapter Specs

<Cards>
  <Card
    title="18 — @stax/claude-code"
    href="/docs/spec/adapter-claude-code"
    description="Exact Claude Code adapter contract"
  />
  <Card
    title="19 — @stax/openclaw"
    href="/docs/spec/adapter-openclaw"
    description="Exact OpenClaw workspace adapter contract"
  />
  <Card
    title="20 — @stax/codex"
    href="/docs/spec/adapter-codex"
    description="Exact Codex adapter contract"
  />
  <Card
    title="33 — @stax/cursor"
    href="/docs/spec/adapter-cursor"
    description="Exact Cursor IDE adapter contract"
  />
  <Card
    title="34 — @stax/github-copilot"
    href="/docs/spec/adapter-github-copilot"
    description="Exact GitHub Copilot adapter contract"
  />
  <Card
    title="35 — @stax/windsurf"
    href="/docs/spec/adapter-windsurf"
    description="Exact Windsurf adapter contract"
  />
  <Card
    title="36 — @stax/opencode"
    href="/docs/spec/adapter-opencode"
    description="Exact OpenCode adapter contract"
  />
</Cards>

### Extended Specs

<Cards>
  <Card
    title="21 — OpenClaw Profile"
    href="/docs/spec/openclaw-profile"
    description="Portable OpenClaw runtime-profile artifact"
  />
  <Card
    title="22 — Workspace Sources"
    href="/docs/spec/workspace-sources"
    description="Cacheable source artifacts and agent references"
  />
  <Card
    title="37 — Subagents and Agent Bundles"
    href="/docs/spec/subagents-and-agent-bundles"
    description="Canonical delegate-agent packaging and handoff metadata"
  />
  <Card
    title="38 — Instruction Trees"
    href="/docs/spec/instruction-trees"
    description="Canonical path-scoped instruction hierarchies"
  />
</Cards>

### Forward-Looking Drafts

<Cards>
  <Card
    title="23 — Registry, Discovery, and Install"
    href="/docs/spec/registry-discovery-install"
    description="Draft: search, install plans, channels, and registry discovery"
  />
  <Card
    title="24 — Trust, Policy, and Attestations"
    href="/docs/spec/trust-policy-attestations"
    description="Draft: signatures, approvals, provenance, revocation, and policy gates"
  />
  <Card
    title="25 — Hosted Platform Adapter"
    href="/docs/spec/hosted-platform-adapter-contract"
    description="Draft: non-filesystem consumers and hosted platform imports"
  />
  <Card
    title="26 — Compatibility Metadata"
    href="/docs/spec/compatibility-and-capability-metadata"
    description="Draft: preflight compatibility and requirement summaries"
  />
  <Card
    title="27 — Package Marketplace Metadata"
    href="/docs/spec/package-and-marketplace-metadata"
    description="Draft: ecosystem and catalog metadata"
  />
  <Card
    title="28 — Conformance and Certification"
    href="/docs/spec/conformance-and-certification"
    description="Draft: interoperability testing and certification"
  />
  <Card
    title="30 — Package and MCP Safety Policy"
    href="/docs/spec/package-and-mcp-safety-policy"
    description="Draft: package and MCP allow/deny policy"
  />
  <Card
    title="99 — Roadmap"
    href="/docs/spec/roadmap"
    description="Status map for 1.0.0, forward drafts, and implementation order"
  />
</Cards>
