---
title: stax login
description: Authenticate with an OCI registry
---

Authenticate with an OCI registry.

## Usage

```bash
stax login ghcr.io
stax login --username user --password-stdin ghcr.io
```

Uses the same credential store as Docker or ORAS when possible.
