{
  "name": "cli",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "bun run src/index.ts",
    "build": "bun build src/index.ts --compile --outfile dist/stax",
    "test": "bun test src/__tests__/",
    "check-types": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/bun": "^1.2.18",
    "typescript": "catalog:"
  }
}
